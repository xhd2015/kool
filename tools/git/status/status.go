package status

// TODO: status --all

// #!/usr/bin/env bash

// script=$(cat << 'EOF'
// dir=${PWD#$FIND_ROOT}
// dir=${dir#'/'}
// if [[ -z $dir ]];then
//     dir=.
// fi

// echo "==== $dir ===="

// git status
// EOF
// )
// FIND_ROOT=$PWD find . -name .git -execdir bash -c "$script" \;
