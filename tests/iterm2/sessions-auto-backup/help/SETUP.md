# Scenario

**Feature**: help surfaces for sessions auto-backup

```
Caller
  -> sessions auto-backup -h | sessions -h
  <- documents interval 10m, --once, default sessions-auto.json; parent lists auto-backup
```
